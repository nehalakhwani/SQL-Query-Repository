SQL Query Repository

A collection of SQL queries demonstrating commonly used SQL functions and clauses.
Covers WHERE, HAVING, CASE WHEN, AVG, MAX, MIN, COUNT(*), and CURDATE().
Includes window functions such as DENSE_RANK() and LAG() for ranking and comparison.
Also includes salary percentile calculations and other practical SQL query examples.
